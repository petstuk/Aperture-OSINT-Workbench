# Aperture v4.0.3

## Improvements

- **IPv6** — full compressed / bracketed / `::ffff:` forms for detect, on-page highlight, labels, and local scope
- **Forum / thread pages** — highlight IoCs inside inline `<code>` and short `<pre>` snippets; watch text edits and open shadow roots
- **Workbench tab icon** — favicon on dashboard and side panel tabs
